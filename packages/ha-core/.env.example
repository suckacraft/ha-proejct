# Copy to ~/ha-core/.env on the Pi and populate before first deploy

# Required
HASS_TOKEN=your_home_assistant_long_lived_access_token

# Optional - defaults shown; override only if your layout differs
DB_PATH=/home/joshsaka/ha-core/data/ha-core.db
CLIENT_CONFIG_PATH=/home/joshsaka/ha-core/client.config.json
SITE_ID=default
HISTORY_RETENTION_DAYS=90
CLIENT_ORIGIN=http://localhost:5173
